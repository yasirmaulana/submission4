<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_change_setting"
        android:title="@string/change_language_settings"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_favorite"
        android:title="@string/favorite"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/action_movie_search"
        android:title="@string/search_movie"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/action_tvshow_search"
        android:title="@string/search_tvshow"
        app:showAsAction="never"
        />
</menu>